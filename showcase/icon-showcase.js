import {showcase} from "project-showcase"


//========================================================
const GROUP_NAME = "Icon";

showcase({ name: "Default", group: GROUP_NAME}, function ($content) {

    $content.innerHTML = `
<i-con from="boxicons" name="ball"></i-con>
`;

});